<Window x:Class="MenadzerWydarzen.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="300" Height="95"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="#2D7DD2" CornerRadius="10" Padding="14,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="Przypomnienie o wydarzeniu"
                       Foreground="White" FontWeight="Bold" FontSize="12"/>

            <Button Grid.Row="0" Grid.Column="1"
                    Content="✕" Width="22" Height="22"
                    Click="CloseButton_Click"
                    Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="11" Cursor="Hand"
                    VerticalAlignment="Top"/>

            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                       x:Name="EventNameText"
                       Foreground="White" FontSize="12" FontWeight="SemiBold"
                       TextWrapping="Wrap" Margin="0,6,0,0"/>

            <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                       x:Name="TimeText"
                       Foreground="#CCEEFF" FontSize="11" Margin="0,3,0,0"/>
        </Grid>
    </Border>
</Window>
