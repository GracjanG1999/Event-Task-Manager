<Window x:Class="MenadzerWydarzen.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="Height"
        Width="320"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="#2D7DD2" CornerRadius="12" Padding="16,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="🔔  Przypomnienie"
                       Foreground="White" FontWeight="Bold" FontSize="12"
                       VerticalAlignment="Center"/>

            <Button Grid.Row="0" Grid.Column="1"
                    Content="✕" Width="24" Height="24"
                    Click="CloseButton_Click"
                    Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="13" Cursor="Hand"
                    VerticalAlignment="Top" Padding="0"/>

            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                       x:Name="EventNameText"
                       Foreground="White" FontSize="13" FontWeight="SemiBold"
                       TextWrapping="Wrap" Margin="0,8,0,0"/>

            <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                       x:Name="TimeText"
                       Foreground="#CCEEFF" FontSize="12" Margin="0,4,0,0"/>
        </Grid>
    </Border>
</Window>
